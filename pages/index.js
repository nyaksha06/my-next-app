export default function Home() {
    return (
        <div style={{ padding: "2rem", fontFamily: "Arial, sans-serif" }}>
            <h1>Hello from Next.js! 👋</h1>
            <p>This is a simple homepage using JavaScript in a TypeScript-based app.</p>
            <p>Try accessing the <code>/api/hello</code> route to see an API response.</p>
        </div>
    );
}
  